package com.highluck.biz;

import java.util.ArrayList;

import javax.servlet.http.HttpServletRequest;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.highluck.dao.DbDAO;
import com.highluck.dto.AlarmDTO;
import com.highluck.dto.DbDTO;


public class DbBiz {
	
/*	private DbDAO dbDao;
	public int x;
	
	
	public void setListDao(DbDAO dbDao){
		this.dbDao = dbDao;		
	}	
	
/	public ArrayList<DbDTO> SelectList() throws Exception{	
		System.out.println(x);
		return (ArrayList<DbDTO>) dbDao.selectMainList(null);
	}
	public ArrayList<DbDTO> SelectList2() throws Exception{	
		System.out.println(x);
		return (ArrayList<DbDTO>) dbDao.selectMainList2(null);
	}
	public ArrayList<DbDTO> SelectList3() throws Exception{	
		System.out.println(x);
		return (ArrayList<DbDTO>) dbDao.selectMainList3(null);
	}public ArrayList<AlarmDTO> SelectList4() throws Exception{	
		System.out.println(x);
		return (ArrayList<AlarmDTO>) dbDao.selectMainList4(null);
	}*/
}
